# Evolution Edge – Local Requirements (CPU / ONNX)
# Install: pip install -r requirements_local.txt
#
# Hardware target: Any CPU (Lenovo LOQ 15ARP9 tested)
# Python: 3.11 or 3.12

# ── Core Inference ─────────────────────────────────────────────────────────
torch>=2.2.0                  # CPU build — no CUDA needed
transformers>=4.40.0
tokenizers>=0.19.0
accelerate>=0.30.0

# ── ONNX Runtime (CPU Execution Provider) ──────────────────────────────────
onnxruntime>=1.18.0           # CPU inference
optimum[onnxruntime]>=1.19.0  # Hugging Face ONNX export + ORT wrapper

# ── Knowledge Distillation (mock fine-tuning on CPU) ───────────────────────
peft>=0.10.0                  # LoRA adapters (used in mock + cloud agent)
datasets>=2.19.0

# ── UI ─────────────────────────────────────────────────────────────────────
gradio>=4.28.0

# ── Utilities ──────────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
requests>=2.31.0
tqdm>=4.66.0
psutil>=5.9.0                 # CPU/RAM monitoring for stats panel
